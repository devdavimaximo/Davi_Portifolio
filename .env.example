# Copy to `.env` and adjust. Only VITE_-prefixed variables reach the browser,
# so nothing secret belongs in this file or in `.env`.

# Deployed origin, no trailing slash.
# Drives canonical URLs, Open Graph tags, JSON-LD and sitemap.xml.
VITE_SITE_URL=https://example.com
